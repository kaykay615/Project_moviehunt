import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.MovieHuntFinalVer.moviehunt',
  appName: 'MovieHunt',
  webDir: 'www'
};

export default config;
